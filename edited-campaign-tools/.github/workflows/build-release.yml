name: Build Foundry release

on:
  workflow_dispatch:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build module ZIP
        run: |
          mkdir -p build/edited-campaign-tools
          rsync -a --exclude '.git' --exclude '.github' --exclude 'build' ./ build/edited-campaign-tools/
          cd build
          zip -r "Edited_Campaign_Tools_Module_v1.0.0.zip" edited-campaign-tools

      - name: Attach ZIP to release
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release upload "v1.0.0" "build/Edited_Campaign_Tools_Module_v1.0.0.zip" --clobber
